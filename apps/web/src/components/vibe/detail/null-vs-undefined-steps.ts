// Data migrated to null-vs-undefined-steps.json for faster TypeScript compilation
import type { DetailStep, StepSection } from '@/components/vibe/detail/KnowledgeDetailPage';
export type { StepSection };
// eslint-disable-next-line @typescript-eslint/no-var-requires
export const STEPS: DetailStep[] = require('./null-vs-undefined-steps.json');
export const TOTAL_STEPS = STEPS.length;
