// Data migrated to oop-prototype-steps.json for faster TypeScript compilation
// eslint-disable-next-line @typescript-eslint/no-var-requires
export const STEPS: any[] = require('./oop-prototype-steps.json');
export const TOTAL_STEPS = STEPS.length;
